module.exports = {
  purge: ["./src/**/*.jsx", "./src/**/*.js"],
  darkMode: "class", // Dark mode enabled! `dark` class must be present atop the HTML tree.
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
